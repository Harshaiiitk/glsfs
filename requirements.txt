# GLSFS - Requirements
# Install with: pip install -r requirements.txt

# Core ML dependencies
torch>=2.0.0
transformers>=4.35.0

# Docker integration
docker>=6.0.0

# Web interface (optional)
gradio>=4.0.0

# Utilities
tqdm>=4.65.0

# Optional: Faster inference (if available)
# unsloth  # Install separately: pip install unsloth
# pip install unsloth --no-deps
# Install docker desktop if previously does not # brew install --cask docker
# pip install torch torchvision sentencepiece